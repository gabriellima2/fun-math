import { BackButton } from "../components/Buttons/BackButton";

import { Common } from "../layouts/Common";

const Learning = () => {
	return (
		<Common>
			<>
				<span className="absolute bottom-12 left-12">
					<BackButton />
				</span>
			</>
		</Common>
	);
};

export default Learning;
