export enum OperatorNames {
	subtraction = "subtraction",
	addition = "addition",
	division = "division",
	multiply = "multiply",
}

export enum OperatorSymbols {
	subtraction = "-",
	addition = "+",
	division = "/",
	multiply = "x",
}
